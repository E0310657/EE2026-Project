#include <bits/stdc++.h>
using namespace std;

int main () {
	int case;
	cin >> case;
	while (case--) {
		int n; cin >> n;
		set<int> man;
		while (n--) {
			int person; cin >> person;
			if (man[person].size() == 1)
			man[p
			else map[person].erase();
		}
	}
	return 0;
}